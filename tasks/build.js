module.exports = function(grunt, configPaths) {
	console.log("build.js Loaded...");
	//configure npm
	var objConfig = {
		//src: 'src/<%= pkg.name %>.js',
		//dest: 'build/<%= pkg.name %>.min.js'
	};
	//load npm
	//
	return objConfig;
}